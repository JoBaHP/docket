/* import { Autosize } from './autosize.directive';

describe('AutosizeDirective', () => {
  it('should create an instance', () => {
    const directive = new Autosize(el: HTMLElement);
    expect(directive).toBeTruthy();
  });
});
 */
